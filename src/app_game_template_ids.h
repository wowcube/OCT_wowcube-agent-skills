// Auto-generated resource IDs for app_game_template
// Replace with actual IDs from the packer tool
#pragma once

#define APP_IDS_H

enum BMP {
    BMP_none = 0,
    BMP_0    = 0,

    BMP_BG     = 1,  // Background
    BMP_TITLE  = 2,  // Menu Title
    BMP_PLAYER = 3,  // Player
    BMP_WIN    = 4,  // Win Image
    BMP_LOOSE  = 5,  // Defeat Image

    BMP_last
};

enum MAP {
    MAP_none = 0,
    MAP_last
};

typedef enum BMP BMP;
typedef enum MAP MAP;
