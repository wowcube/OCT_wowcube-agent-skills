# Exact pins on purpose: the packer's output is compared byte-for-byte against
# the reference toolchain (docs/part3-parity-report.md), and a quantiser or
# PNG-decoder change in a dependency moves those bytes. Bump deliberately, in
# a commit that re-runs the parity measurements.

# Image processing (used by build_psd.py, pack.py, unpack.py)
Pillow==12.2.0
numpy==2.4.4

# PSD file writing (used by build_psd.py)
pytoshop==1.2.1

# PSD file reading (used by pack.py)
psd-tools==1.9.34

# HTTP client for AI sprite generation (used by skills/cube_asset-builder/scripts/genimg.py)
requests==2.34.2
